// CANVAS SIZE & CALLING ID="CANVAS"
var canvas = document.getElementById('canvas'),
	c = canvas.getContext('2d');
var innerWidth = 360,
	innerHeight = 620;
	canvas.width = innerWidth,
	canvas.height = innerHeight;


// KEY MAPPING 